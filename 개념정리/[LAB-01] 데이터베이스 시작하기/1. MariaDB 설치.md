# MariaDB 설치

스터디 기간 동안 **MariaDB**를 사용한다.

## MariaDB

MySQL에서 파생된 **오픈소스 관계형 데이터베이스 관리 시스템(RDBMS)**

## MariaDB 접속

```bash
mariadb -uroot -p --host=localhost --port=9090
```

## 데이터베이스 선택

```sql
USE myschool;
```

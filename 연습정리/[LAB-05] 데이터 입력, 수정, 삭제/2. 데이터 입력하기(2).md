# 데이터 입력하기 (2)

## 01

students 테이블에 학생을 추가하되, 성별은 ‘남’, 상태는 ‘재학’으로 저장하세요.  
입학일은 오늘 날짜로 저장하며, 키/몸무게는 임의로 설정하세요. 그 밖의 다른 필수 항목은 임의의 데이터로 저장하세요.

```sql
INSERT INTO students (name, user_id, grade, idnum, birthdate, phone, height, weight, gender, status, admission_date, department_id) VALUES ('김현욱', 'kimhw', 2, '4566274598562', '1993-05-05', '010-8273-8493', 173, 70, '남', '재학', NOW(), 506);
SELECT * FROM students ORDER BY id DESC LIMIT 0, 3;
```

---

## 02

101번 학과의 박태수 부교수의 이메일 주소는 'PARK@MYSCHOOL.AC.KR'이다.  
이메일을 모두 소문자로 변환해서 등록하세요. 그 밖의 정보는 임의로 저장하세요.

```sql
INSERT INTO professors (name, user_id, position, sal, hiredate, email, status, department_id) VALUES ('박태수', 'parkts', '전임강사', 300, '2025-07-14', LOWER('PARK@MYSCHOOL.AC.KR'), '재직', 505);
SELECT * FROM professors ORDER BY id DESC LIMIT 0, 3;
```

---

## 03

학생 이름이 '김하늘', 소속 학과명이 '소프트웨어공학과'인 학생을 등록하세요.  
이메일은 이름+소속학과명+글자3개@myschool.ac.kr 형식으로 생성하며, 전부 소문자로 저장하면 됩니다.  
예: kimhn소프트@myschool.ac.kr → kimhn소프트@myschool.ac.kr → 소문자 변환  
그 밖의 정보는 임의로 설정하여 저장하세요.

```sql
INSERT INTO students (name, user_id, grade, idnum, birthdate, phone, height, weight, email, department_id) VALUES ('김하늘', 'kimhn', 3, 'srvbdwewiioufyghjk', '2000-08-26', '035-7432-9507', 150, 45, 'kimhn소프트@myschool.ac.kr', 506);
SELECT * FROM students ORDER BY id DESC LIMIT 0, 3;
```

---

## 04

enrollments 테이블은 student_id와 subject_id가 참조키입니다.  
존재하지 않는 학생 번호와 과목 번호로 수강신청을 시도해보세요.

```sql
INSERT INTO enrollments (student_id, subject_id, enroll_date) VALUES (99999, 99999, NOW());
```

```text
ERROR 1452 (23000): Cannot add or update a child row: a foreign key constraint fails (`myschool`.`enrollments`, CONSTRAINT `enrollments_ibfk_1` FOREIGN KEY (`student_id`) REFERENCES `students` (`id`))
```

---

## 05

students 테이블에 성별을 '남성'으로 잘못 입력하면 어떤 일이 발생하는지 확인해보세요.

```sql
INSERT INTO students (name, user_id, grade, idnum, birthdate, phone, height, weight, gender, status, admission_date, department_id) VALUES ('김남수', 'kimhw', 2, '4566274598562', '1993-05-05', '010-8273-8493', 173, 70, '남성', '재학', NOW(), 506);
```

```text
ERROR 1265 (01000): Data truncated for column 'gender' at row 1
```
